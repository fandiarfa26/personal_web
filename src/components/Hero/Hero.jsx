import React from 'react'
import HeroWrapper from './HeroWrapper';

const Hero = () => {
  return (
    <section id="hero" className="relative h-screen page-section">
      <HeroWrapper/>
    </section>
  )
}

export default Hero